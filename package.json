{
  "name": "qs-connect",
  "version": "0.1.0",
  "description": "QS Connect — the global network connecting university-verified talent with employers",
  "main": "server/index.js",
  "scripts": {
    "start": "node start.js",
    "build:client": "cd client && npm install --no-audit --no-fund && npm run build"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.10.0",
    "express": "^4.21.2",
    "express-session": "^1.18.1"
  }
}
